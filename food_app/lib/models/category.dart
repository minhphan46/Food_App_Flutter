import 'package:flutter/material.dart';

class Category {
  // cac the loai mon an ( model )
  final int id;
  final String content;
  final Color color;

  const Category({
    required this.id,
    required this.content,
    required this.color,
  });
}
